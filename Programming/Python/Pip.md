# Cheat codes (Python Pip)


| Description                                       |                       commands                        |
|:-------------------------------------------------:|:-----------------------------------------------------:|
| Create requirements.txt | `pip3 list --format=freeze > requirements.txt` <br> `conda env export > environment.yml --no-builds`|